<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-permission android:name="android.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        android:required="false" tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BaiTapQuaTrinh3"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ChildrenActivity.ReminderActivity"
            android:exported="true">

        </activity>
        <activity
            android:name=".ChildrenActivity.ReminderListActivity"
            android:exported="true">
        </activity>
        <receiver
            android:name=".CalendarReminder.AlarmReceiver"
            android:enabled="true"
            android:exported="false" />
        <!-- ContentProvider -->
        <provider
            android:name=".ContentProvider.ImageProvider"
            android:authorities="com.example.baitapquatrinh3.provider"
            android:exported="true"
            android:grantUriPermissions="true">
        </provider>
        <activity android:name=".ChildrenActivity.ImageDetailActivity" />



    </application>

</manifest>
